input_path: data/preprocessed
output_path: data/uppercase-scored
model_path: models/supervised-uppercase
charset: uppercase
training:
  batch_size: 64
  epochs: 50
  steps_per_epoch: 5000
  optimizer:
    class: Adam
    kwargs:
      learning_rate: 0.001
  loss:
    class: CategoricalCrossentropy
  metrics:
  - accuracy
  callbacks:
  - class: TensorBoard
    kwargs:
      log_dir: logs/tensorboard/supervised-uppercase
  - class: ReduceLROnPlateau
    kwargs:
      min_lr: 0.0001
      factor: 0.5
      patience: 2
      monitor: accuracy
model:
  path: models/supervised-uppercase